<section class="Section Section--spacingNormal home_blog_section" id="section-{{ section.id }}" data-section-id="{{ section.id }}" data-section-type="slideshow">
  <div class="Container">
    {% if section.settings.title != blank %}
    <h2>{{ section.settings.title }}</h2>
    {% endif %}
    <div class="home_blog">
      {% assign blog = blogs[section.settings.blog] %}
      {% for article in blog.articles limit:1 %}
      <div class="home_blog_left">
        <figure><img src="{{ article.image | img_url: 'master' }}" /></figure>
        <div class="home_blog_left_desc">
          <h4><a href="{{ article.url }}">{{ article.title }}</a></h4>
          {{ article.content }}
        </div>
      </div>
      {% endfor %}
      <div class="home_blog_right">
        <div class="home_blog_right_list">
        </div>
      </div>
    </div>
  </div>
</section>

{% schema %}
  {
    "name": "Home Blog",
    "settings": [
      {
        "type": "checkbox",
        "id": "enabled_home_blog",
        "label": "Show Home Blog",
        "default": true
      },
      {
        "type": "text",
        "id": "title",
        "label": "Heading"
      },
      {
        "type": "blog",
        "id": "blog",
        "label": "Blog"
      }
    ],
    "presets": [
      {
        "name": "Home Blog",
        "category": "Blog"
      }
    ]
  }
{% endschema %}

{% stylesheet %}
{% endstylesheet %}

{% javascript %}
{% endjavascript %}