<section class="Section Section--spacingNormal product_use_it_section" id="section-{{ section.id }}" data-section-id="{{ section.id }}">
  <div class="Container">
    <div class="product_use_it">
      {% if product.metafields.custom.use_as_you_heading != blank %}
      <h2>{{ product.metafields.custom.use_as_you_heading }}</h2>
      {% endif %}
      {% assign i = -1 %}
      {% for image in product.metafields.custom.use_as_you_image.value %}
        {% assign i = i | plus:1 %}
      <div class="product_use_it_wrapper">
        {% if image != blank %}
        <figure><img src="{{ image | img_url: 'master' }}" /></figure>
        {% endif %}
        {% if product.metafields.custom.use_as_you_text.value[i] != blank %}
          <h3>{{ product.metafields.custom.use_as_you_text.value[i] }}</h3>
        {% endif %}
        {% if product.metafields.custom.use_as_you_desc.value[i] != blank %}
          <p>{{ product.metafields.custom.use_as_you_desc.value[i] }}</p>
        {% endif %}
      </div>
        {% endfor %}
    </div>
  </div>
</section>


{% schema %}
  {
    "name": "Use It As You Like",
    "settings": [
      {
        "type": "header",
        "content": "Content In Metafield"
      }
    ],
    "presets": [
      {
        "name": "Use It As You Like",
        "category": "Text"
      }
    ]
  }
{% endschema %}
